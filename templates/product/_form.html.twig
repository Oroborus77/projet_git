{{ form_start(form) }}
    {{ form_widget(form) }}
    <button style="display:flex;justify-items: center;justify-content: center;align-items: center;width: 160px;margin-left: auto;margin-right: auto;margin-top: 15px;margin-bottom: 15px;font-weight: bold" class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
